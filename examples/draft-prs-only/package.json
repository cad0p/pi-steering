{
  "name": "@examples/draft-prs-only",
  "version": "0.0.0-example",
  "private": true,
  "description": "Rule pack: gh pr create must be --draft.",
  "type": "module",
  "peerDependencies": {
    "@cad0p/pi-steering": "workspace:*"
  },
  "devDependencies": {
    "@cad0p/pi-steering": "workspace:*"
  },
  "scripts": {
    "pretest": "pnpm --filter @cad0p/pi-steering run clean && pnpm --filter @cad0p/pi-steering run build",
    "test": "node --test 'steering.test.ts'",
    "typecheck": "tsc --noEmit"
  }
}
